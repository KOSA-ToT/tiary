server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://3.37.222.161:3306/tiary?characterEncoding=UTF-8
    username: root
    password: tiary1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.springframework: INFO
    com.example.myapp: DEBUG